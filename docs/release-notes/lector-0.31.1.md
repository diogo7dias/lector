## Fixed
- **In-book menu section headings match the rest of the UI.** Sleep, Device, TOOLS and every other list section is the classic full-width black bar with centred white text, not a left-aligned underline. Controls (In Book / Outside Book) and sync comparison labels (Theirs / Mine, Remote / Local) use the same bar.

## Notes
- Same two images as 0.31.0: `firmware.bin` for X3/X4, `firmware-x4pro.bin` for X4 Pro. An X4 Pro already on 0.30.0–0.31.0 can OTA this.
