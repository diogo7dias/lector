#!/bin/bash

set -e

cd "$(dirname "$0")/../builtinFonts"

echo "// The contents of this file are generated by ./lib/EpdFont/scripts/build-font-ids.sh"
echo "#pragma once"
echo ""

echo "#define VOLLKORN_12_FONT_ID ($(
ruby -rdigest -e 'puts [
  "./vollkorn_12_regular.h",
  "./vollkorn_12_bold.h",
  "./vollkorn_12_bolditalic.h",
  "./vollkorn_12_italic.h",
].map{|f| Digest::SHA256.hexdigest(File.read(f)).to_i(16) }.sum % (2 ** 32) - (2 ** 31)'
))"

echo "#define VOLLKORN_14_FONT_ID ($(
ruby -rdigest -e 'puts [
  "./vollkorn_14_regular.h",
  "./vollkorn_14_bold.h",
  "./vollkorn_14_bolditalic.h",
  "./vollkorn_14_italic.h",
].map{|f| Digest::SHA256.hexdigest(File.read(f)).to_i(16) }.sum % (2 ** 32) - (2 ** 31)'
))"

echo "#define VOLLKORN_16_FONT_ID ($(
ruby -rdigest -e 'puts [
  "./vollkorn_16_regular.h",
  "./vollkorn_16_bold.h",
  "./vollkorn_16_bolditalic.h",
  "./vollkorn_16_italic.h",
].map{|f| Digest::SHA256.hexdigest(File.read(f)).to_i(16) }.sum % (2 ** 32) - (2 ** 31)'
))"

echo "#define VOLLKORN_18_FONT_ID ($(
ruby -rdigest -e 'puts [
  "./vollkorn_18_regular.h",
  "./vollkorn_18_bold.h",
  "./vollkorn_18_bolditalic.h",
  "./vollkorn_18_italic.h",
].map{|f| Digest::SHA256.hexdigest(File.read(f)).to_i(16) }.sum % (2 ** 32) - (2 ** 31)'
))"

echo "#define UI_10_FONT_ID ($(
ruby -rdigest -e 'puts [
  "./ubuntu_10_regular.h",
  "./ubuntu_10_bold.h",
].map{|f| Digest::SHA256.hexdigest(File.read(f)).to_i(16) }.sum % (2 ** 32) - (2 ** 31)'
))"

echo "#define UI_12_FONT_ID ($(
ruby -rdigest -e 'puts [
  "./ubuntu_12_regular.h",
  "./ubuntu_12_bold.h",
].map{|f| Digest::SHA256.hexdigest(File.read(f)).to_i(16) }.sum % (2 ** 32) - (2 ** 31)'
))"

echo "#define SMALL_FONT_ID ($(
ruby -rdigest -e 'puts [
  "./notosans_8_regular.h",
].map{|f| Digest::SHA256.hexdigest(File.read(f)).to_i(16) }.sum % (2 ** 32) - (2 ** 31)'
))"
